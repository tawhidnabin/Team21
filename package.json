{
  "name": "smart-attendance",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "preview": "vite preview",
    "postinstall": "chmod +x ./node_modules/.bin/vite || true"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-qr-code": "^2.0.6",  // or your desired version
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  },
  "engines": {
    "node": "16.x"
  }
}
